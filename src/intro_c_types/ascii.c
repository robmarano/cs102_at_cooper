#include <stdio.h>

int main() {
	char c; /* variable declaration */

	c = 'D'; /* variable definition */

	printf("c = %c (decimal)\n", c);
	printf("c = %x (hex)\n", c);
	printf("c = %o (octal)\n", c);

	return 0;
}
